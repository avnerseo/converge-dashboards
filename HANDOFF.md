# HANDOFF — converge-dashboards

מסמך המשכיות. סשן חדש שנפתח על הריפו הזה אמור לקרוא אותו ראשון ולדעת מיד מה המצב.

**עודכן לאחרונה:** 31.8.2026, ~18:30 UTC · ענף `main` בקומיט `ec3f249`

---

## מצב נוכחי

| | |
|---|---|
| `index.html` — דשבורד מניות | מעודכן ל-31.8.2026. **כל 25 המחירים חיים.** מתוחזק. |
| `crypto.html` — דשבורד קריפטו | **לא מתוחזק בכוונה.** נתונים מ-20.8. יש באנר אדום בראש הדף שאומר זאת. |
| `free-claude-code/` | כלי עזר, לא חלק מהדשבורדים. ראה למטה. |

---

## הדבר החשוב ביותר לדעת: מאיפה מגיעים מחירים

**מכסת Alpha Vantage החינמית (25 קריאות/יום) נגמרת כמעט בכל יום, לעיתים לפני תחילת ההרצה.** אל תבזבז את ההרצה על ניסיונות חוזרים.

**הפתרון שעובד:** מחבר **Bigdata.com** מחזיר ציטוטי מחיר חיים ואינו כפוף למכסה הזאת.

```
find_securities(query="<שם החברה>")           → קח את ה-id של התוצאה עם listing_type PUBLIC
bigdata_company_tearsheet(rp_entity_id=<id>,
    company_type="Public", sections=["company_overview"])
```

השדות הרלוונטיים בתשובה: `price`, `change_percentage`, `timestamp`, וגם `year_low`/`year_high`
(שימושי לזיהוי מניה שנסחרת סמוך לשפל). המקור בפועל הוא FMP.

**מקורות שנבדקו ואינם זמינים מסביבת ההרצה** (חסומים ע"י מדיניות ה-egress) — אל תנסה שוב:
`finance.yahoo.com`, `stockanalysis.com`, `finnhub.io`, `api.twelvedata.com`,
`api.polygon.io`, `financialmodelingprep.com`, `zacks.com`. גם `huggingface.co` חסום.
`github.com` ו-`pypi.org` כן פתוחים.

---

## עבודה מקבילה — לקרוא לפני שנוגעים ב-index.html

לחשבון הזה יש **כמה סשנים שרצים במקביל על אותו ריפו**, וחלקם מריצים עדכון יומי משלהם.
ב-31.8 היו שני עדכונים מתחרים לאותו יום, ותיקיית העבודה המקומית של המשתמש הוחלפה לענף
של סשן אחר באמצע העבודה.

**כללי עבודה שנגזרו מזה:**

1. לפני עריכה — `git fetch origin main` ולבדוק אם `main` זז.
2. לעבוד על ענף נפרד, לא ישירות על `main`.
3. **עריכות ממוקדות בלבד.** לא לכתוב מחדש את כל הקובץ — זה מה שהופך התנגשות
   קטנה למיזוג בלתי אפשרי.
4. רגע לפני הדחיפה — לבדוק שוב אם `main` זז. אם כן, למזג ולהחיל מחדש, לא לדרוס.
5. אם סשן אחר כבר עשה מחקר טוב יותר לאותו יום — לשמר את שלו ולהוסיף רק את מה שחסר לו.
   כך נעשה ב-`9e137ce`: נשמר המחקר של הסשן האחר, והוספו עליו רק המחירים.

---

## `free-claude-code/` — מה זה ולמה

כלי שמאפשר להריץ את ה-CLI של Claude Code מול פרוקסי מקומי שמנתב למודל של ספק אחר,
כדי שתהיה דרך להמשיך לעבוד כשמכסת המנוי נגמרת. **זה לא נותן את Claude** — זה נותן את
הממשק עם מודל אחר וחלש יותר מאחוריו.

`fcc.sh` (macOS/Linux) ו-`fcc.ps1` (Windows) — פקודות: `setup`, `start`, `claude`, `api`,
`testkey`, `status`, `stop`, `uninstall`. המדריך המלא ב-`free-claude-code/README.md`,
כולל ארבעת המכשולים שעלו בהתקנה אמיתית על Windows.

**מותקן ועובד אצל המשתמש** תחת `~/Documents/fcc`, עם מפתח NVIDIA NIM.

**מסקנה מהשימוש בפועל:** המודל החינמי (nemotron) עונה, אבל נותן הוראות שגויות בביטחון
(פקודות לינוקס למשתמש Windows) והעברית שלו מוצגת הפוכה. **לא להריץ דרכו את העדכון היומי.**
לעבודה אמיתית: `.\fcc.ps1 api` עם מפתח Anthropic בתשלום, או להמתין לאיפוס המכסה.

---

## מה פתוח

- **`crypto.html`** — הפרויקט מושהה בהחלטת המשתמש. לא לעדכן בלי שיבקש. הבאנר מונע הטעיה בינתיים.
- **שאר טבלת המתודולוגיה** — 48 מטבעות בקריפטו ורוב 77 המניות בטבלאות אינם מקבלים מחיר חי.
  רק כרטיסי שכבה 1 / משנית / סיכון גבוה מתעדכנים.
- **מכסת Alpha Vantage** — צוואר בקבוק קבוע. חבילה בתשלום היא ‎$49.99 לחודש, מה שלא הומלץ.
  Bigdata פותר את זה בחינם, ראה למעלה.

---

## הערה על אמינות

הדשבורד הזה מוצג למישהו שמקבל לפיו החלטות. **לעולם לא להמציא מספר.** שדה שאין לו נתון
מסומן במפורש כלא זמין, וסעיף §שקיפות בתחתית `index.html` מתעד מה נכשל בכל הרצה —
כולל כישלונות של הסוכן עצמו. אם מגבלה שתועדה שם כבר לא קיימת, לתקן את הניסוח ולא למחוק
אותו: דף שמצהיר על מגבלה שאיננה מטעה בדיוק כמו דף שמסתיר מגבלה שקיימת.
